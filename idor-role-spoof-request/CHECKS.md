# CHECKS — idor-role-spoof-request

## Команды запуска
```
docker compose config --quiet
docker compose up --build -d
bash solution/solution.sh
```

## Результаты самопроверки
Флоу login -> patch -> export прогнан локально с эквивалентным Flask
test client (среда подготовки без Docker) 10/10 раз успешно, флаг
получен во всех прогонах при точной последовательности из README.

- Прогонов: 10
- Успешных: 10
- Ограничения: состояние сервиса не персистентно между перезапусками
  контейнера (in-memory dict) — это ожидаемо и не мешает решению.
- Внешние зависимости: не требуются.
